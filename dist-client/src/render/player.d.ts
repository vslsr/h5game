import type { Player } from '../../shared/types';
export declare function drawPlayer(ctx: CanvasRenderingContext2D, player: Player, isSelf: boolean, camX: number, camY: number, width: number, height: number): void;
